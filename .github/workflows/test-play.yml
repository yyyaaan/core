name: Test Play App

on:
  pull_request:
    paths:
      - 'apps/py-play/**'
      - 'packages/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.github/workflows/test-play.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Check lockfile
        run: uv lock --check
        
      - name: Install dependencies
        run: uv sync

      - name: Run Ruff check
        run: uv run ruff check apps/py-play/

      - name: Run Pytest
        run: uv run pytest apps/py-play/tests/
